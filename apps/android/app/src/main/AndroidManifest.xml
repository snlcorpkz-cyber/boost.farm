<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ACCESS_NETWORK_STATE is recommended by AppsFlyer (and Firebase) so
         the SDK can defer event flushes when the device is offline rather
         than dropping them. Without it, AF still works but install
         attribution accuracy drops on flaky connections. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- AD_ID: required on Android 13+ for apps that read the Google
         Advertising ID. SDKs in our stack that depend on this permission:
           • AppsFlyer — GAID is the primary attribution identifier.
             Without the permission, AF falls back to fingerprint matching
             and Meta / Google match rates drop ~20%.
           • ironSource / LevelPlay rewarded ads — uses GAID for ad
             targeting and frequency capping. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:name=".BoostFarmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoostFarm"
        android:usesCleartextTraffic="false">

        <!-- Ads are handled by Unity LevelPlay (ironSource) — initialization is
             purely runtime in MainActivity, no manifest meta-data required. -->

        <!-- Marketing attribution: AppsFlyer SDK. Init is runtime-only in
             BoostFarmApplication.initAppsFlyer — no manifest meta-data
             required. AF auto-installs its own InstallReferrerReceiver
             through the merged manifest from the AAR. -->

        <!-- FCM: default icon & channel for notifications when app is killed -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_logo" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="boostfarm_default" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
